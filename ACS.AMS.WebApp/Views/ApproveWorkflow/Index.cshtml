@model IndexPageModel
@{
    ViewBag.Title = "Index";
}

        <div class="list-container">
              @{
        DisplayHelper display = new DisplayHelper(this, "ApproveWorkflow", "ApproveWorkflowID");

        var gridCtrl = Html.Kendo().Grid<dynamic>()
                .Name("DetailsGrid");

        display.AddGridActionColumns(gridCtrl, Url, "ApproveWorkflow", RightNames.ApproveWorkflow);
            display.ConfigureGrid(gridCtrl);
        display.AddGridHeaders(gridCtrl, Url, this.Html, RightNames.ApproveWorkflow);

            gridCtrl.Render();
            //display.AddPopUpWindow(this.Html, "Division");

        }
         
        </div>
   
<script>
$(function () {
        setPageHeading("Approval Workflow List");
});
</script>
